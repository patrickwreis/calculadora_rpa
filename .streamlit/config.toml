[theme]
base = "dark"
primaryColor = "#0B69FF"
backgroundColor = "#0A0F1A"
secondaryBackgroundColor = "#0F1724"
textColor = "#E6EEF8"
font = "sans serif"

# Optional: set wide layout as default for a dashboard feel
[global]
email = ""

[server]
# Increase server.maxUploadSizeMB if you allow file uploads in the app
# maxUploadSizeMB = 200
